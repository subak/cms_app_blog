listen: 80
hosts:
  default:
    paths:
      /:
        file.dir: html/public
        mruby.handler-file: web/ruby/handler.rb
        file.dir: content
        file.dir: /tmp/cms
        file.dir: html/stylesheets

access-log: /dev/stdout
error-log: /dev/stderr
user: root